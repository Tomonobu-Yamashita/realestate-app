# realestate-app プロジェクト ガイドライン

## プロジェクト概要

不動産物件管理・検索アプリケーション。

---

## Git 運用ルール

- **コードを変更するたびに GitHub にプッシュすること**
  - 変更 → `git add` → `git commit` → `git push` を毎回実行する
  - コミットメッセージは変更内容を端的に日本語で記述する
  - 例: `feat: 物件検索フィルター機能を追加`
- ブランチ戦略
  - `main`: 本番相当の安定ブランチ
  - `feature/<機能名>`: 機能開発用ブランチ（マージ後は削除）
- コミットメッセージのプレフィックス
  - `feat:` 新機能
  - `fix:` バグ修正
  - `refactor:` リファクタリング
  - `docs:` ドキュメント変更
  - `chore:` 設定・依存関係など

---

## ファイル・フォルダ規則

- ファイルの削除・上書き前には必ず確認を求めること
- フォルダ名・ファイル名の日付は `YYYYMMDD` 形式にすること
- 作業が完了したら `Claude_Work/作業ログ/` フォルダに実施内容を追記すること

---

## コーディング規則

- コメントは原則書かない。書く場合は「なぜそうするか（WHY）」のみ1行で
- エラーハンドリングはシステム境界（ユーザー入力・外部API）のみに絞る
- セキュリティ: SQL インジェクション・XSS 等 OWASP Top 10 に注意する
- 不要な抽象化・将来の拡張を見越した設計は行わない（YAGNI）

---

## 作業完了時のチェックリスト

1. コードの動作確認
2. `git add` → `git commit` → `git push` を実行
3. `Claude_Work/作業ログ/` に作業内容を追記
